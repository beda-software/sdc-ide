name: actions-demo
on: [push]
jobs:
  Build-New-Image:
    runs-on: ubuntu-latest
    steps:
        - run: echo "GITHUB_SHA $GITHUB_SHA"
        - run: echo "GITHUB_REF $GITHUB_REF"
        - run: echo "start building new image"
        - run: export TIER=staging
        - run: docker build -t test-release:test-tag2 .
        -
          name: Login to Docker Hub
          uses: docker/login-action@v1
          with:
            username: atuonufure
            password: dockerdocker1
        - run: docker tag test-release:test-tag2 atuonufure/test-release:test-tag2
        - run: docker push atuonufure/test-release:test-tag2


      # username: ${{ secrets.DOCKERHUB_USERNAME }}
      # password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      # - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      # - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      # - name: Check out repository code
      #   uses: actions/checkout@v2
      # - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      # - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      # - name: List files in the repository
      #   run: |
      #     ls ${{ github.workspace }}
      # - run: echo "🍏 This job's status is ${{ job.status }}."
